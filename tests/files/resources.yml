storage:

  lvm_storage:
    driver: lvm

  file_storage:
    driver: file



networks:

  prod-1:
    driver: bridge
    openvswitch: true
    bridge: sw0
    vlan: 10

  prod-2:
    driver: bridge
    openvswitch: true
    bridge: sw0
    vlan: 20

  labs:
    driver: bridge
    bridge: sw1
